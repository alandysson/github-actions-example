name: First Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Build Docker image
        run: |
          sudo docker build -t ${{ vars.IMAGE_ADDRESS }} .
          sudo docker push ${{ vars.IMAGE_ADDRESS }}
